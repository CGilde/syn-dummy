_schema-version: '2.0'
ID: syn-dummy
version: 0.0.1

modules:
 - name: db
   type: hdb
   path: db
   requires:
    - name: hdi-container

resources:
 - name: hdi-container
   type: com.sap.xs.hdi-container
